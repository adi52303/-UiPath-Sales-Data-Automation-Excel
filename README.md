# -UiPath-Sales-Data-Automation-Excel
This UiPath project automates the processing and visualization of sales data. It reads raw data from an Excel file, processes it to compute metrics like Total Sales, filters based on business rules, and generates automated Excel — all without manual effort.
